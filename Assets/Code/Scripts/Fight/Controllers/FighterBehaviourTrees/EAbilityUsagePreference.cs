namespace FrostfallSaga.Fight.Controllers.FighterBehaviourTrees
{
    /// <summary>
    /// Enum that represents the preference of the fighter when using an ability.
    /// </summary>
    public enum EAbilityUsagePreference
    {
        MAXIMIZE_EFFECT,
        MINIMIZE_COST,
        RANDOM,
    }
}